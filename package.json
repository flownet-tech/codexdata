{
  "name": "codex-models",
  "version": "0.1.0",
  "private": true,
  "description": "Codex Models — independent mirror of the official OpenAI Codex model catalog (snapshots + change feed) plus curated third-party model profiles for the Codex client, served as static JSON from Cloudflare Workers. Not affiliated with OpenAI.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.12.3",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "seed": "node scripts/seed.mjs",
    "check": "pnpm typecheck && pnpm format:check && pnpm test"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "zod": "^4.5.4"
  }
}
