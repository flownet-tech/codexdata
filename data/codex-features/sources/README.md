# Sources

Verbatim copies of `codex-rs/features/src/lib.rs` from https://github.com/openai/codex at the tags
named by the directories, plus `codex-rs/features/src/legacy.rs` (kept once at this level because it
is byte-identical across every verified tag). Used to derive `../registry.json` (the feature-flag
registry) and as test vectors. They are licensed under the Apache License 2.0 by OpenAI; see
`LICENSE` and `NOTICE` in this directory. Not affiliated with OpenAI.
